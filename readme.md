Made for CS4218. (zaidan)

Installation:
1. Install `uv`. https://docs.astral.sh/uv/getting-started/installation/
2. `uv sync` to install dependencies.
3. Run the app:
- `uv run python desktop.py` to run the desktop app.
- `uv run python app/app.py` to run the web app.

